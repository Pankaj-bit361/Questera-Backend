container_commands:
  01_link_pm2:
    command: |
      if [ -n "$PM2_PUBLIC_KEY" ] && [ -n "$PM2_SECRET_KEY" ]; then
        pm2 link $PM2_SECRET_KEY $PM2_PUBLIC_KEY
      fi
    ignoreErrors: true
    leader_only: true

